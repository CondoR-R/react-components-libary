# Библиотека компонентов React + TypeScript

## Button

### Варианты исполнения

По дефолту кнопка окрашивается в цвет `--color-primary`, цвет текста `--color-light`.
При указании `outline` цвет фона станет `--color-light`, а цвет текста `--color-primary`
